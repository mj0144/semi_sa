<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="member">
 <select id="login" parameterType="mem" resultType="String">
	select user_num from user_info where user_id=#{user_id} and pwd=#{pwd}
 </select>
 
 <select id="viewUser" resultType="mem">
select user_id, nickname, pwd, name, sex,info_year, info_mon, info_day, info_hh, info_mm, user_img, ilju from user_info where user_id=#{user_id} and pwd=#{pwd}
 </select>
 
</mapper>
